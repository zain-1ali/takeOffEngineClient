# Public URL of the live API (Railway), no trailing slash.
# Sheet preview PNGs are loaded from `${VITE_API_BASE_URL}/uploads/...`
# Also set the same host in vercel.json (/api and /uploads rewrites).
VITE_API_BASE_URL=https://take-off-engine-server-three.vercel.app

