# Copy to .env.local for local Google Sign-In
VITE_GOOGLE_CLIENT_ID=
# Production: public URL of the Railway (or other) API, no trailing slash.
# Leave empty locally so /api and /uploads use the Vite proxy.
# VITE_API_BASE_URL=https://your-api.up.railway.app

